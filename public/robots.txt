User-agent: *
Allow: /

# Preview account surfaces use page-level noindex metadata so crawlers can
# actually read the directive. A sitemap will be added once the free public
# Pages hostname is created.
