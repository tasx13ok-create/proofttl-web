(() => {
'use strict'
const E=window.OntologyEngineV2
const $=s=>document.querySelector(s)
const KEY='reality-engine-representation-audit-v2'
const randSeed=()=>{const a=new Uint32Array(1);crypto.getRandomValues(a);return a[0]||1}
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))
const pct=n=>Number.isFinite(n)?`${(n*100).toFixed(1)}%`:'—'
const num=(n,d=3)=>Number.isFinite(n)?Number(n).toFixed(d):'—'
const sci=n=>Number.isFinite(n)?(n<.001?n.toExponential(1):n.toFixed(4)):'—'
const state={ontology:E.seedOntology(),depth:0,attack:0,attempt:0,lineageSeed:randSeed(),lineage:[],trace:[],auto:false,busy:false,last:null,tests:[],animation:null,requestId:0}
let worker=null

function log(msg){state.trace.unshift({at:Date.now(),msg:String(msg)});state.trace=state.trace.slice(0,180);renderTrace()}
function save(){try{localStorage.setItem(KEY,JSON.stringify({ontology:state.ontology,depth:state.depth,attack:state.attack,attempt:state.attempt,lineageSeed:state.lineageSeed,lineage:state.lineage.slice(-120),last:state.last}))}catch{}}
function restore(){try{const d=JSON.parse(localStorage.getItem(KEY)||'null');if(!d)return;state.ontology=Array.isArray(d.ontology)&&d.ontology.length?d.ontology:E.seedOntology();state.depth=Number(d.depth)||0;state.attack=Number(d.attack)||0;state.attempt=Number(d.attempt)||0;state.lineageSeed=Number(d.lineageSeed)||randSeed();state.lineage=Array.isArray(d.lineage)?d.lineage:[];state.last=d.last||null;log(`MEMORY · restored ${state.ontology.length} senses · depth ${state.depth}`)}catch(e){log(`MEMORY · restore skipped · ${e.message}`)}}
function nextAttackSeed(){state.attempt++;let x=(state.lineageSeed ^ Math.imul(state.attempt,0x9E3779B1) ^ Math.imul(state.attack+1,0x85EBCA6B))>>>0;x^=x>>>16;x=Math.imul(x,0x7feb352d);x^=x>>>15;x=Math.imul(x,0x846ca68b);x^=x>>>16;return x>>>0||1}

function bootWorker(){if(worker)worker.terminate();worker=new Worker('./ontology-worker-v2.js');worker.onmessage=onWorker;worker.onerror=e=>{state.busy=false;$('#engine-pill').textContent='WORKER ERROR';$('#engine-pill').className='pill bad';log(`WORKER ERROR · ${e.message}`);renderButtons()};worker.postMessage({type:'selftest'})}
function onWorker(e){const m=e.data||{};if(m.type==='selftest'){state.tests=m.tests||[];renderTests();$('#engine-pill').textContent='ENGINE READY';$('#engine-pill').className='pill good';return}if(m.type==='progress'){const phase=m.phase==='twin'?`TWIN FORGE ${String(m.family||'').toUpperCase()} r${m.restart||1}/${m.restarts||1} g${m.generation}`:`INSTRUMENT FORGE g${m.generation}`;$('#engine-pill').textContent=phase;$('#engine-pill').className='pill warn';if(m.phase==='instrument'&&m.formula){$('#formula').textContent=m.formula;$('#certificate').textContent='EVOLVING';$('#certificate').className='pill warn'}return}if(m.type==='error'){state.busy=false;$('#engine-pill').textContent='ENGINE ERROR';$('#engine-pill').className='pill bad';log(`ENGINE ERROR · ${m.message}`);renderButtons();if(state.auto)setTimeout(runAttack,1200);return}if(m.type==='cycle'){state.busy=false;handleResult(m.result);renderButtons();if(state.auto)setTimeout(runAttack,m.result?.ok?900:450)}}

function runAttack(){if(state.busy)return;state.busy=true;const seed=nextAttackSeed(),requestId=++state.requestId;$('#engine-pill').textContent='ATTACK STARTING';$('#engine-pill').className='pill warn';$('#attack-label').textContent=`ATTACK ${state.attack+1} · SEED ${seed.toString(16).toUpperCase()}`;log(`ATTACK · ${state.attack+1} · locked split seed ${seed.toString(16).toUpperCase()}`);renderButtons();worker.postMessage({type:'cycle',ontology:state.ontology,depth:state.depth,attack:state.attack,seed,requestId})}
function handleResult(r){if(!r)return;state.last=r;state.attack=Number(r.attack??state.attack+1);if(r.ok){state.ontology=r.ontology;state.depth=r.depth;const c=r.winner?.certificate||{},inst=r.winner?.instrument;state.lineage.push({at:Date.now(),attack:state.attack,depth:state.depth,reason:r.reason,instrument:inst?.label||'',id:inst?.id||'',tier:inst?.tier||'',baseline:r.baseline?.certified??null,localGain:c.accuracyGain,transferGain:c.transferAccuracyGain});if(r.strictDepth)log(`DEPTH EVENT · ${inst.id} · 0/${r.baseline.count} random probes passed · depth ${state.depth}`);else log(`DERIVED SENSE · ${inst.id} · ${r.baseline.certified}/${r.baseline.count} random probes also passed · depth unchanged`)}else{state.lineage.push({at:Date.now(),attack:state.attack,depth:state.depth,reason:r.reason});if(r.reason==='attack_resisted')log('ATTACK RESISTED · twin audit refused promotion');else log('INSTRUMENT REJECTED · no finalist survived certification')}save();renderAll();setAnimation(r);$('#engine-pill').textContent=r.ok?(r.strictDepth?'DEPTH INCREASED':'DERIVED SENSE ADDED'):(r.reason==='attack_resisted'?'ATTACK RESISTED':'CERTIFICATION FAILED');$('#engine-pill').className=`pill ${r.ok?(r.strictDepth?'good':'warn'):'bad'}`}

function renderButtons(){$('#attack-btn').disabled=state.busy;$('#attack-btn').textContent=state.busy?'Attack running…':'Attack current science';$('#auto-btn').textContent=`Auto evolve: ${state.auto?'on':'off'}`;$('#auto-btn').className=`btn ${state.auto?'primary':''}`}
function renderStats(){const derived=state.ontology.filter(x=>x.tier==='derived-dsl').length;$('#depth').textContent=state.depth;$('#depth-pill').textContent=`DEPTH ${state.depth}`;$('#senses').textContent=state.ontology.length;$('#derived').textContent=derived;$('#operator-pressure').textContent=`${derived} derived sense${derived===1?'':'s'} currently encoded in the fixed DSL`;$('#lineage-id').textContent=state.lineageSeed.toString(16).toUpperCase().padStart(8,'0')}
function renderTwin(){const r=state.last;if(!r?.primary){for(const id of ['hidden-distance','ontology-accuracy','ontology-gap','transfer-twin-accuracy'])$('#'+id).textContent='—';return}const p=r.primary,t=r.transfer;$('#family-a').textContent=`PRIMARY / ${p.family.toUpperCase()}`;$('#family-b').textContent=`PRIMARY / ${p.family.toUpperCase()}`;$('#hash-a').textContent=p.hashA;$('#hash-b').textContent=p.hashB;$('#hidden-distance').textContent=num(p.hiddenDistance);$('#ontology-accuracy').textContent=pct(p.auditAccuracy);$('#ontology-gap').textContent=num(p.auditGap);$('#transfer-twin-accuracy').textContent=t?pct(t.auditAccuracy):'—';$('#twin-acc').textContent=pct(p.auditAccuracy);$('#twin-status').textContent=p.qualifies?'TWIN':'RESISTED';$('#twin-status').style.color=p.qualifies?'var(--green)':'var(--red)'}
function renderCertificate(){const r=state.last,w=r?.winner,c=w?.certificate,b=r?.baseline;if(!w||!c){$('#formula').textContent=r?.reason==='attack_resisted'?'No candidate forged because the twin attack failed the locked audit.':'No certified instrument yet.';$('#instrument-id').textContent='—';$('#certificate').textContent=r?.reason==='attack_resisted'?'ATTACK RESISTED':'NO CANDIDATE';$('#certificate').className=`pill ${r?'bad':''}`;for(const id of ['c-effect','c-acc','c-irr','c-p','c-pred','t-effect','t-acc','t-irr'])$('#'+id).textContent='—';$('#baseline-count').textContent='—';$('#depth-verdict').textContent='NO PROMOTION';return}const inst=w.instrument||{};$('#formula').textContent=inst.label||E.instrumentString(w.program);$('#instrument-id').textContent=inst.id||'FINALIST';$('#c-effect').textContent=`${num(c.effect,2)}σ`;$('#c-acc').textContent=`+${pct(c.accuracyGain)}`;$('#c-irr').textContent=num(c.irreducibility,2);$('#c-p').textContent=sci(c.padj);$('#c-pred').textContent=(c.predictiveGain>=0?'+':'')+pct(c.predictiveGain);$('#t-effect').textContent=`${num(c.transferEffect,2)}σ`;$('#t-acc').textContent=`+${pct(c.transferAccuracyGain)}`;$('#t-irr').textContent=num(c.transferIrreducibility,2);$('#baseline-count').textContent=`${b?.certified??0} / ${b?.count??32} passed`;if(r.strictDepth){$('#certificate').textContent='STRICT DEPTH EVENT';$('#certificate').className='pill good';$('#depth-verdict').textContent='REPRESENTATION EVENT';$('#instrument-note').textContent='The sense passed both simulator families and no fresh random DSL program passed the same certification.'}else if(r.ok){$('#certificate').textContent='DERIVED SENSE';$('#certificate').className='pill warn';$('#depth-verdict').textContent='ONTOLOGY +1 / DEPTH +0';$('#instrument-note').textContent='Useful and cross-simulator, but the current DSL could produce other passing measurements by random search. Added to science; not counted as epistemic depth.'}else{$('#certificate').textContent='REJECTED';$('#certificate').className='pill bad';$('#depth-verdict').textContent='NO PROMOTION'}}
function renderSplits(){const p=state.last?.primary?.splitHashes,c=state.last?.winner?.certificate?.splitHashes;$('#split-search').textContent=p?.search||'—';$('#split-audit').textContent=p?.audit||'—';$('#split-cert').textContent=c?.cert||'—';$('#split-pred').textContent=c?.prediction||'—';$('#split-transfer').textContent=c?.transfer||'—'}
function renderOntology(){const host=$('#ontology');host.innerHTML=state.ontology.map((x,i)=>`<div class="sense"><span class="sid">${esc(x.id||`S${i}`)}</span><span class="tier">${esc((x.tier||'seed').toUpperCase())}</span><code>${esc(x.label||E.instrumentString(x.program))}</code></div>`).join('')}
function renderLineage(){const host=$('#lineage');if(!state.lineage.length){host.innerHTML='<div class="event"><b>No attacks yet.</b><small>The ledger records failures too.</small></div>';return}host.innerHTML=[...state.lineage].reverse().slice(0,36).map(x=>`<div class="event"><b>${esc(x.reason.replaceAll('_',' ').toUpperCase())}</b>${x.id?` · ${esc(x.id)}`:''}<small>attack ${x.attack} · depth ${x.depth}${x.baseline!=null?` · random passes ${x.baseline}/32`:''}</small></div>`).join('')}
function renderTrace(){$('#trace').innerHTML=state.trace.map(x=>`<div><time>${new Date(x.at).toLocaleTimeString()}</time>${esc(x.msg)}</div>`).join('')}
function renderTests(){const host=$('#tests');host.innerHTML=(state.tests||[]).map(t=>`<div class="test ${t.pass?'pass':'fail'}"><b>${t.pass?'PASS':'FAIL'} · ${esc(t.name)}</b><span>${esc(t.detail)}</span></div>`).join('')||'<div class="test"><b>Waiting for worker tests…</b></div>'}
function renderAll(){renderStats();renderTwin();renderCertificate();renderSplits();renderOntology();renderLineage();renderTrace();renderButtons()}

function setAnimation(r){if(!r?.primary)return;try{const seed=(state.lineageSeed^state.attack^0xC011AB1E)>>>0,a=E.trajectory(r.primary.family,r.primary.a,seed,'observe',120,3),b=E.trajectory(r.primary.family,r.primary.b,seed,'observe',120,3);state.animation={family:r.primary.family,a,b,index:0,last:0}}catch(e){log(`VISUALIZATION · ${e.message}`)}}
function drawFrame(canvas,frame,family){if(!frame)return;const dpr=Math.min(2,devicePixelRatio||1),rect=canvas.getBoundingClientRect(),w=Math.max(280,Math.round(rect.width*dpr)),h=Math.max(220,Math.round(rect.height*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}const ctx=canvas.getContext('2d');ctx.fillStyle='#03070a';ctx.fillRect(0,0,w,h);ctx.strokeStyle='rgba(80,180,205,.05)';for(let x=0;x<w;x+=70*dpr){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke()}const n=frame.x.length,px=i=>(frame.x[i]*.42+.5)*w,py=i=>(frame.y[i]*.42+.5)*h;if(family==='graph'){ctx.strokeStyle='rgba(108,231,245,.14)';for(let i=0;i<n;i++){ctx.beginPath();ctx.moveTo(px(i),py(i));ctx.lineTo(px((i+1)%n),py((i+1)%n));ctx.stroke()}}for(let i=0;i<n;i++){const speed=Math.hypot(frame.vx[i],frame.vy[i]);ctx.beginPath();ctx.arc(px(i),py(i),(1.7+Math.min(3,speed*4))*dpr,0,Math.PI*2);ctx.fillStyle=`rgba(${120+Math.min(100,Math.round(speed*180))},220,240,${.42+Math.min(.45,speed)})`;ctx.fill()}}
function loop(ts){if(state.animation){if(ts-state.animation.last>85){state.animation.last=ts;state.animation.index=(state.animation.index+1)%Math.min(state.animation.a.length,state.animation.b.length)}drawFrame($('#world-a'),state.animation.a[state.animation.index],state.animation.family);drawFrame($('#world-b'),state.animation.b[state.animation.index],state.animation.family)}else{drawEmpty($('#world-a'));drawEmpty($('#world-b'))}requestAnimationFrame(loop)}
function drawEmpty(c){const r=c.getBoundingClientRect(),dpr=Math.min(2,devicePixelRatio||1),w=Math.max(280,Math.round(r.width*dpr)),h=Math.max(220,Math.round(r.height*dpr));if(c.width!==w||c.height!==h){c.width=w;c.height=h}const x=c.getContext('2d');x.fillStyle='#03070a';x.fillRect(0,0,w,h);x.fillStyle='#37535d';x.textAlign='center';x.font=`${12*dpr}px monospace`;x.fillText('WAITING FOR EPISTEMIC TWIN',w/2,h/2)}

function exportEvidence(){const payload={product:'Reality Engine — Representation Audit',version:E.VERSION,exportedAt:new Date().toISOString(),lineageSeed:state.lineageSeed,depth:state.depth,attack:state.attack,ontology:state.ontology,lineage:state.lineage,lastResult:state.last,tests:state.tests};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`reality-engine-evidence-${state.lineageSeed.toString(16)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);log('EXPORT · evidence package created')}
function newLineage(){if(state.busy)return;state.ontology=E.seedOntology();state.depth=0;state.attack=0;state.attempt=0;state.lineageSeed=randSeed();state.lineage=[];state.last=null;state.animation=null;localStorage.removeItem(KEY);log(`GENESIS · new lineage ${state.lineageSeed.toString(16).toUpperCase()}`);save();renderAll()}
function bind(){$('#attack-btn').onclick=runAttack;$('#auto-btn').onclick=()=>{state.auto=!state.auto;renderButtons();log(`AUTO · ${state.auto?'enabled':'disabled'}`);if(state.auto&&!state.busy)runAttack()};$('#export-btn').onclick=exportEvidence;$('#new-btn').onclick=newLineage;$('#tests-btn').onclick=()=>worker.postMessage({type:'selftest'})}
function init(){restore();bind();bootWorker();renderAll();if(state.last)setAnimation(state.last);requestAnimationFrame(loop);log(`SYSTEM · Representation Audit ${E.VERSION} ready`)}
document.addEventListener('DOMContentLoaded',init)
window.__REALITY_ENGINE_AUDIT__={getState:()=>({depth:state.depth,attack:state.attack,ontology:state.ontology.length,busy:state.busy,lastReason:state.last?.reason||null}),attack:runAttack,selfTest:()=>worker.postMessage({type:'selftest'}),exportEvidence}
})()
