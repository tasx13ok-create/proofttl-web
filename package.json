{
  "name": "proofttl-web",
  "version": "1.0.1",
  "private": true,
  "description": "Official ProofTTL website for the $1,500 Fact Audit: source-backed verification of high-consequence AI outputs and factual claims with human approval and a seven-day watch.",
  "homepage": "https://proofttl-web.vercel.app/",
  "repository": {
    "type": "git",
    "url": "https://github.com/tasx13ok-create/proofttl-web.git"
  },
  "keywords": [
    "ProofTTL",
    "Fact Audit",
    "ai-fact-audit",
    "claim-verification",
    "fact-checking",
    "ai-claim-verification",
    "source-backed-verification",
    "adversarial-verification",
    "human-approved-verification"
  ],
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "node scripts/check-static-export.js && node scripts/check-discovery-identity.js && node scripts/check-assistant-session.js && node scripts/check-sales-intake.js && node scripts/check-auth-trust.js && node scripts/check-product-1000.js && node scripts/check-public-sales-shell.js && node scripts/check-mobile-sales-layout.js && node scripts/check-reality-engine.cjs",
    "start": "npx serve out",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build",
    "check:export": "npm run check",
    "check:product": "node scripts/check-product-1000.js",
    "check:discovery": "node scripts/check-discovery-identity.js",
    "check:reality": "node scripts/check-reality-engine.cjs",
    "discovery:indexnow": "node scripts/indexnow-submit.js",
    "discovery:websub": "node scripts/websub-publish.js"
  },
  "dependencies": {
    "@better-auth/core": "1.6.25",
    "@better-auth/passkey": "1.6.25",
    "better-auth": "1.6.25",
    "next": "16.3.0",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "typescript": "5.7.3"
  }
}
